# Automatically created when a Vercel Blob store is connected to the project.
BLOB_READ_WRITE_TOKEN=

# Generate these once and add them to Vercel Project Settings → Environment Variables.
# APP_PASSWORD_HASH uses the format: scrypt$<salt hex>$<hash hex>
APP_PASSWORD_HASH=
SESSION_SECRET=
